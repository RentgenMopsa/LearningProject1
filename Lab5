#include <stdio.h>
void main(){
    int x;
    int sum =0;
    printf("x = ");
    scanf("%d", &x);
    while (x >= 0) {
        sum=sum+x;
        printf("x = ");
        scanf("%d", &x);
    }
    printf("sum = %d", sum);
}
